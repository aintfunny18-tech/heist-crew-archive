# Deployment

This standalone repository publishes through GitHub Actions. The deployment
workflow validates the static source, builds `_site/`, and deploys that artifact
to GitHub Pages.

Production URL:

`https://aintfunny18-tech.github.io/heist-crew-archive/`

The migration does not alter the F4 Prestige Archive application or its routes.
